# 🔄 СИНХРОНИЗАЦИЯ С ЯНДЕКС.ДИСКОМ

## 📋 Как работает синхронизация

---

## 🎯 Процесс синхронизации

### 1. Сохранение данных на Яндекс.Диск

**Файлы для синхронизации:**
```javascript
[
    'coop_members.json',       // Пайщики
    'coop_payments.json',      // Взносы
    'coop_transactions.json',  // Проводки
    'coop_documents.json',     // Документы
    'coop_applications.json',  // Заявления
    'coop_meetings.json',      // Собрания
    'coop_certificates.json',  // Удостоверения
    'coop_settings.json'       // Настройки
]
```

**Процесс:**
```javascript
1. Чтение данных из localStorage
2. Преобразование в JSON
3. Отправка на Яндекс.Диск через API
4. Подтверждение сохранения
```

---

### 2. Загрузка данных с Яндекс.Диска

**Процесс:**
```javascript
1. Запрос списка файлов на Яндекс.Диске
2. Загрузка каждого файла
3. Парсинг JSON
4. Сохранение в localStorage
5. Обновление UI
```

---

### 3. Резервная копия

**Создание backup:**
```javascript
{
    timestamp: "2026-02-21T11-00-00-000Z",
    members: [...],
    payments: [...],
    transactions: [...],
    documents: [...],
    applications: [...],
    meetings: [...],
    certificates: [...],
    settings: {...}
}
```

**Имя файла:** `backup_2026-02-21T11-00-00-000Z.json`

**Хранение:** `/КООПЕРАНТ/Backup/`

---

## 📁 Структура на Яндекс.Диске

```
/КООПЕРАНТ/
├── coop_members.json          # Пайщики
├── coop_payments.json         # Взносы
├── coop_transactions.json     # Проводки
├── coop_documents.json        # Документы
├── coop_applications.json     # Заявления
├── coop_meetings.json         # Собрания
├── coop_certificates.json     # Удостоверения
├── coop_settings.json         # Настройки
└── Backup/
    ├── backup_2026-02-21T11-00-00-000Z.json
    ├── backup_2026-02-21T12-00-00-000Z.json
    └── ...
```

---

## 🔧 Функции синхронизации

### `saveAllDataToYandex()`

Сохраняет все данные на Яндекс.Диск.

**Процесс:**
1. Проверка что синхронизация не выполняется
2. Чтение данных из localStorage
3. Отправка каждого файла на Яндекс.Диск
4. Подсчёт сохранённых файлов
5. Обновление времени последней синхронизации

**Пример:**
```javascript
await window.saveAllDataToYandex();
// ✅ Сохранено 8 из 8 файлов
```

---

### `loadAllDataFromYandex()`

Загружает все данные с Яндекс.Диска.

**Процесс:**
1. Проверка что синхронизация не выполняется
2. Запрос каждого файла с Яндекс.Диска
3. Парсинг JSON
4. Сохранение в localStorage
5. Обновление UI

**Пример:**
```javascript
await window.loadAllDataFromYandex();
// ✅ Загружено 8 из 8 файлов
```

---

### `createBackupYandex()`

Создаёт резервную копию всех данных.

**Процесс:**
1. Сбор всех данных из localStorage
2. Создание JSON объекта с timestamp
3. Сохранение в папку `/Backup/`
4. Удаление старых копий (если > 10)

**Пример:**
```javascript
await window.createBackupYandex();
// ✅ Резервная копия создана: backup_2026-02-21T11-00-00-000Z.json
```

---

### `syncFileToYandex(key, fileName)`

Синхронизирует один файл.

**Параметры:**
- `key`: Ключ в localStorage (например, 'members')
- `fileName`: Имя файла на Яндекс.Диске

**Пример:**
```javascript
await window.syncFileToYandex('members', 'coop_members.json');
// ✅ Файл coop_members.json синхронизирован
```

---

## ⏰ Автосохранение

### Интервал
**30 секунд** (настраивается в `YANDEX_DISK_CONFIG.AUTO_SAVE_INTERVAL`)

### Процесс
```javascript
setInterval(async () => {
    if (!isSyncing && cooperativFolderId && yandexDiskToken) {
        await saveAllDataToYandex();
    }
}, 30000);
```

---

## 🧪 Тестирование

### Откройте тестовую страницу
```
test-sync.html
```

### Проверка подключения
```javascript
// Проверка токена и папки
checkConnection();
```

### Сохранение данных
```javascript
// Сохранить все данные
saveToYandex();
```

### Загрузка данных
```javascript
// Загрузить все данные
loadFromYandex();
```

### Создание резервной копии
```javascript
// Создать backup
createBackup();
```

---

## 📊 Логи синхронизации

### Успешная синхронизация
```
[Yandex] Сохранение данных в Яндекс Диск...
[Yandex] ✅ coop_members.json сохранён
[Yandex] ✅ coop_payments.json сохранён
[Yandex] ✅ coop_transactions.json сохранён
[Yandex] ✅ coop_documents.json сохранён
[Yandex] ✅ coop_applications.json сохранён
[Yandex] ✅ coop_meetings.json сохранён
[Yandex] ✅ coop_certificates.json сохранён
[Yandex] ✅ coop_settings.json сохранён
[Yandex] Сохранено 8 из 8 файлов
```

### Ошибка синхронизации
```
[Yandex] ⚠️ coop_members.json не сохранён
[Yandex] ❌ Ошибка сохранения данных
```

---

## 🐛 Решение проблем

### "Требуется авторизация"
**Причина:** Нет токена  
**Решение:** Пройдите авторизацию

### "Папка не найдена"
**Причина:** Folder ID не сохранён  
**Решение:** Подключите Яндекс.Диск заново

### "Синхронизация уже выполняется"
**Причина:** Предыдущая синхронизация не завершилась  
**Решение:** Подождите 5-10 секунд

### "Ошибка сохранения файла"
**Причина:** Нет доступа к диску  
**Решение:** Проверьте права доступа в OAuth Console

---

## ✅ Проверка синхронизации

### 1. Откройте тестовую страницу
```
test-sync.html
```

### 2. Проверьте подключение
```
🔍 Проверить подключение
```

### 3. Сохраните данные
```
💾 Сохранить на Яндекс.Диск
```

### 4. Проверьте на Яндекс.Диске
Откройте https://disk.yandex.ru и проверьте папку `/КООПЕРАНТ/`

### 5. Загрузите данные
```
📥 Загрузить с Яндекс.Диска
```

---

**Версия:** 2.7  
**Дата:** 21 февраля 2026  
**Статус:** ✅ Синхронизация работает
