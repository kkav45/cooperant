# ИСПРАВЛЕНИЕ ОШИБКИ АВТОРИЗАЦИИ YANDEX DISK

**Дата:** 18 февраля 2026  
**Проблема:** `redirect_uri не совпадает с Callback URL`

---

## 🚨 БЫСТРОЕ РЕШЕНИЕ

### 1. Откройте Яндекс OAuth Console

Перейдите на: **https://oauth.yandex.ru/client/new**

### 2. Создайте новое приложение (или обновите существующее)

**Настройки:**
```
Название: Кооператив - Учёт пайщиков
Платформа: Веб-сервисы
Redirect URI: https://honor1506.github.io/koop/yandex-auth-callback.html
Права доступа: cloud_api:disk.app_folder ✓
```

### 3. Скопируйте Client ID и Client Secret

После создания приложения вы получите:
- **Client ID** (ID приложения)
- **Client Secret** (Секретный ключ)

### 4. Обновите файл `yandex-disk-integration-v2.js`

Найдите строки 36-50 и замените:

```javascript
const YANDEX_DISK_CONFIG = {
    CLIENT_ID: 'bd5a4cf12f1a4fcfabfd14e82ed12db5',        // Ваш Client ID
    CLIENT_SECRET: '5f3a4cb70ca1497aaffda8d2e4e985d6',     // Ваш Client Secret
    REDIRECT_URI: 'https://honor1506.github.io/koop/yandex-auth-callback.html',
    // ...
};
```

### 5. Закоммитьте изменения в GitHub

```bash
git add yandex-disk-integration-v2.js
git commit -m "Update Yandex OAuth configuration"
git push
```

### 6. Подождите 1-2 минуты

GitHub Pages обновится автоматически

### 7. Протестируйте авторизацию

1. Откройте: https://honor1506.github.io/koop/messenger_interface.html
2. Нажмите на кнопку "Яндекс Диск" в левом меню
3. Нажмите "Войти через Яндекс"
4. Разрешите доступ
5. Проверьте успех

---

## 📁 ЧТО БЫЛО ИЗМЕНЕНО

### Новые файлы

| Файл | Назначение |
|------|------------|
| `yandex-auth-callback.html` | Страница обработки авторизации |
| `YANDEX_OAUTH_SETUP.md` | Подробная инструкция по настройке |
| `YANDEX_DISK_BUTTON_GUIDE.md` | Руководство по кнопке Яндекс.Диска |

### Обновлённые файлы

| Файл | Изменения |
|------|-----------|
| `yandex-disk-integration-v2.js` | Обновлён REDIRECT_URI, добавлена отладка |
| `messenger_interface.html` | Добавлена кнопка Яндекс.Диска |

---

## 🔍 ПРИЧИНЫ ПРОБЛЕМЫ

### 1. Неправильный Redirect URI

**Было:**
```javascript
REDIRECT_URI: window.location.origin + window.location.pathname
// Результат: https://honor1506.github.io/koop/messenger_interface.html
```

**Стало:**
```javascript
REDIRECT_URI: 'https://honor1506.github.io/koop/yandex-auth-callback.html'
```

### 2. Отсутствие callback страницы

**Решение:** Создан файл `yandex-auth-callback.html`

---

## 🎨 КАК ЭТО РАБОТАЕТ

### Схема авторизации

```
1. Пользователь нажимает "Яндекс Диск"
         ↓
2. Открывается окно авторизации Яндекса
         ↓
3. Пользователь разрешает доступ
         ↓
4. Яндекс перенаправляет на:
   https://honor1506.github.io/koop/yandex-auth-callback.html#access_token=XXX
         ↓
5. yandex-auth-callback.html извлекает токен
         ↓
6. Токен сохраняется в localStorage
         ↓
7. Окно закрывается, показывается успех
         ↓
8. Приложение загружает данные с Яндекс.Диска
```

---

## 🧪 ОТЛАДКА

### Включите логирование

В начале файла `yandex-disk-integration-v2.js`:

```javascript
window.YANDEX_DEBUG = true;
```

### Проверьте консоль

Откройте консоль браузера (F12) и проверьте логи:

```
[Yandex] Текущий URL: https://honor1506.github.io/koop/messenger_interface.html
[Yandex] Configured Redirect URI: https://honor1506.github.io/koop/yandex-auth-callback.html
[Yandex] Открытие окна авторизации...
```

### Проверьте токены

В консоли выполните:

```javascript
// Проверка токена
localStorage.getItem('yandexDiskToken')

// Проверка папки
localStorage.getItem('yandexCooperativFolderId')
```

---

## 📊 ПРОВЕРКА НАСТРОЕК

### В Яндекс OAuth Console

1. Перейдите на https://oauth.yandex.ru/client/your_client_id
2. Проверьте **Redirect URI**:
   ```
   https://honor1506.github.io/koop/yandex-auth-callback.html
   ```
3. Проверьте **Права доступа**:
   ```
   ✓ cloud_api:disk.app_folder
   ```

### В браузере

1. Откройте https://honor1506.github.io/koop/yandex-auth-callback.html
2. Страница должна загрузиться (показать "Обработка авторизации...")

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Точное совпадение URL

Redirect URI в Яндекс Console должен **ТОЧНО** совпадать с `yandex-disk-integration-v2.js`:

✅ **Правильно:**
```
https://honor1506.github.io/koop/yandex-auth-callback.html
```

❌ **Неправильно:**
```
https://honor1506.github.io/KOOP/yandex-auth-callback.html  (заглавные буквы)
https://honor1506.github.io/koop/                            (нет файла)
http://honor1506.github.io/koop/yandex-auth-callback.html   (HTTP вместо HTTPS)
```

### 2. GitHub Pages чувствителен к регистру

- `koop` ≠ `KOOP`
- Используйте только строчные буквы в URL

### 3. HTTPS обязателен

Яндекс OAuth требует HTTPS для Redirect URI

GitHub Pages предоставляет HTTPS автоматически

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ

### Для локальной разработки

Если тестируете локально, добавьте в `yandex-disk-integration-v2.js`:

```javascript
REDIRECT_URI: 'http://localhost:8080/yandex-auth-callback.html',
```

И в Яндекс Console укажите тот же URL.

### Для Vercel/Netlify

```javascript
REDIRECT_URI: 'https://your-app.vercel.app/yandex-auth-callback.html',
```

---

## 📞 ЕСЛИ ВСЁ ЕЩЁ НЕ РАБОТАЕТ

### 1. Проверьте Client ID

Убедитесь, что используете правильный Client ID:

```javascript
console.log('Client ID:', YANDEX_DISK_CONFIG.CLIENT_ID);
```

### 2. Проверьте права доступа

Убедитесь, что выбрано право `cloud_api:disk.app_folder`

### 3. Очистите кэш

```javascript
localStorage.clear();
location.reload();
```

### 4. Попробуйте другой браузер

Некоторые браузеры могут блокировать redirect

### 5. Проверьте консоль Яндекса

Перейдите на https://oauth.yandex.ru/ и проверьте логи приложения

---

## ✅ УСПЕШНАЯ АВТОРИЗАЦИЯ

После успешной авторизации вы увидите:

```
[Yandex] ✓ Яндекс Диск авторизация успешна
[Yandex] ✓ Папка КООПЕРАНТ найдена
[Yandex] ✓ Загрузка данных из Яндекс Диска...
[Yandex] ✓ Все данные загружены
```

В Яндекс.Диске создастся структура папок:

```
КООПЕРАНТ/
├── Data/
│   ├── coop_members.json
│   ├── coop_payments.json
│   └── ...
├── Applications/
├── Certificates/
├── Protocols/
├── Documents/
├── Backup/
├── Reports/
└── Exports/
```

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- `YANDEX_OAUTH_SETUP.md` — Подробная настройка OAuth
- `YANDEX_DISK_BUTTON_GUIDE.md` — Руководство по кнопке
- `DEPLOYMENT_GUIDE.md` — Развёртывание приложения
- `TELEGRAM_THEMEPARAMS_INTEGRATION.md` — Интеграция с Telegram

---

**Контакты для поддержки:** Проверьте логи в консоли браузера
