# 📁 СОЗДАНИЕ ПУСТЫХ ФАЙЛОВ НА ЯНДЕКС.ДИСКЕ

**Задача:** 1.4  
**Статус:** ⏳ Требуется ручное выполнение

---

## 🎯 ЦЕЛЬ

Создать пустые JSON файлы на Яндекс.Диске для корректной работы автосохранения.

---

## 📊 ЧТО НУЖНО СОЗДАТЬ

**Папка:** `Приложения/КООПЕРАНТ/Data/`

**Файлы:**
1. `coop_members.json` — Пустой массив `[]`
2. `coop_payments.json` — Пустой массив `[]`
3. `coop_transactions.json` — Пустой массив `[]`
4. `coop_documents.json` — Пустой массив `[]`
5. `coop_applications.json` — Пустой массив `[]`
6. `coop_meetings.json` — Пустой массив `[]`
7. `coop_certificates.json` — Пустой массив `[]`
8. `coop_settings.json` — Пустой объект `{}`

---

## 🔧 СПОСОБ 1: ВРУЧНУЮ (Рекомендуется)

### Шаг 1: Откройте Яндекс.Диск

Перейдите: https://disk.yandex.ru/client/app

### Шаг 2: Найдите папку КООПЕРАНТ

```
Яндекс.Диск → Приложения → КООПЕРАНТ → Data
```

### Шаг 3: Создайте файлы

1. Нажмите **"Создать"** → **"Текстовый файл"**
2. Назовите файл: `coop_members.json`
3. Откройте файл и впишите: `[]`
4. Сохраните

Повторите для всех файлов.

---

## 🔧 СПОСОБ 2: ЧЕРЕЗ API (Для продвинутых)

### Скрипт для создания файлов

```javascript
// Выполните в консоли браузера на disk.yandex.ru
const files = [
    'coop_members.json',
    'coop_payments.json',
    'coop_transactions.json',
    'coop_documents.json',
    'coop_applications.json',
    'coop_meetings.json',
    'coop_certificates.json',
    'coop_settings.json'
];

const emptyContent = {
    'coop_settings.json': '{}',
    default: '[]'
};

async function createFiles() {
    const token = 'YOUR_OAUTH_TOKEN';  // Получите в Яндекс OAuth
    
    for (const file of files) {
        const content = emptyContent[file] || emptyContent.default;
        
        await fetch(`https://cloud-api.yandex.net/v1/disk/resources/upload?path=app:/КООПЕРАНТ/Data/${file}&overwrite=true`, {
            method: 'POST',
            headers: {
                'Authorization': `OAuth ${token}`,
                'Content-Type': 'application/json'
            },
            body: content
        });
        
        console.log(`✅ Создан: ${file}`);
    }
}

createFiles();
```

---

## ✅ ПРОВЕРКА

После создания файлов:

1. **Откройте консоль** (F12)
2. **Обновите страницу** (F5)
3. **Проверьте логи:**

```
[Yandex] Файл coop_members.json найден
[Yandex] Файл coop_payments.json найден
...
[Yandex] Загружено 8 из 8 файлов ✅
```

---

## ⚠️ ВАЖНО

1. **Путь к папке:** `Приложения/КООПЕРАНТ/Data/` (не в корне!)
2. **Формат файлов:** JSON (UTF-8)
3. **Содержимое:**
   - Массивы: `[]`
   - Настройки: `{}`

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

```
Приложения/
└── КООПЕРАНТ/
    └── Data/
        ├── coop_members.json          []
        ├── coop_payments.json         []
        ├── coop_transactions.json     []
        ├── coop_documents.json        []
        ├── coop_applications.json     []
        ├── coop_meetings.json         []
        ├── coop_certificates.json     []
        └── coop_settings.json         {}
```

---

**После создания файлов — обновите страницу и проверьте загрузку!** ✅
