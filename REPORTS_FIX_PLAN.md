# 🔧 ИСПРАВЛЕНИЯ ОТЧЁТОВ (РЕАЛЬНЫЕ ДАННЫЕ)

**Дата:** 19 февраля 2026  
**Статус:** Требуется реализация

---

## 📋 ПРОБЛЕМА

Все отчёты используют **примерные данные**, а не реальные из базы:

```javascript
// Сейчас (ПЛОХО):
const balanceData = {
    assets: { value: 150000 }  // Примерные данные!
};

// Нужно (ХОРОШО):
const transactions = JSON.parse(localStorage.getItem('transactions') || '[]');
const payments = JSON.parse(localStorage.getItem('payments') || '[]');
// Реальные данные из базы!
```

---

## 🔧 ТРЕБУЕМЫЕ ИСПРАВЛЕНИЯ

### 1. Бухгалтерский баланс (showBalanceReport)

**Файл:** `index.html` (строка ~2862)

**Что исправить:**

```javascript
// БЫЛО:
const balanceData = {
    assets: { nonCurrent: { value: 150000 } }  // Примерно
};

// СТАЛО:
const transactions = JSON.parse(localStorage.getItem('transactions') || '[]');
const payments = JSON.parse(localStorage.getItem('payments') || '[]');

// Считаем остатки по счетам
const balances = {};
transactions.forEach(t => {
    balances[t.debitAccount] = (balances[t.debitAccount] || 0) + t.amount;
    balances[t.creditAccount] = (balances[t.creditAccount] || 0) - t.amount;
});

// Реальные данные
const cash = Math.abs(balances['50'] || 0) + Math.abs(balances['51'] || 0);
const contributions = payments.reduce((sum, p) => sum + p.amount, 0);
```

---

### 2. ОСВ (showOSVReport)

**Файл:** `index.html` (строка ~2970)

**Что исправить:**

```javascript
// БЫЛО:
const osvData = [
    { account: '50', debitStart: 15000 }  // Примерно
];

// СТАЛО:
const transactions = JSON.parse(localStorage.getItem('transactions') || '[]');

// Группируем по счетам
const accounts = {};
transactions.forEach(t => {
    if (!accounts[t.debitAccount]) accounts[t.debitAccount] = { debit: 0, credit: 0 };
    if (!accounts[t.creditAccount]) accounts[t.creditAccount] = { debit: 0, credit: 0 };
    
    accounts[t.debitAccount].debit += t.amount;
    accounts[t.creditAccount].credit += t.amount;
});

// Преобразуем в массив для таблицы
const osvData = Object.keys(accounts).map(code => ({
    account: code,
    debitTurn: accounts[code].debit,
    creditTurn: accounts[code].credit
}));
```

---

### 3. КУДиР (showKUDiRReport)

**Файл:** `index.html` (строка ~3160)

**ЧТО УЖЕ ИСПРАВЛЕНО:** ✅
- Использует реальные данные из `payments`
- Фильтрует за текущий год
- Считает доходы по типам

**ЧТО ДОБАВИТЬ:**
- Расходы из проводок (счета 20, 26, 44)
- Налоговая база = Доходы - Расходы

---

### 4. Форма №2 (showProfitLossReport)

**Файл:** `index.html` (строка ~3280)

**ЧТО УЖЕ ИСПРАВЛЕНО:** ✅
- Использует реальные данные из `payments`
- Считает доходы по типам

**ЧТО ДОБАВИТЬ:**
- Расходы из проводок
- Чистая прибыль = Доходы - Расходы

---

### 5. РСВ (showRSVReport)

**Файл:** `index.html` (строка ~3370)

**ПРОБЛЕМА:** Примерные данные

**ЧТО ИСПРАВИТЬ:**

```javascript
// БЫЛО:
const rsvData = {
    employees: 3,  // Примерно!
    totalIncome: 450000  // Примерно!
};

// СТАЛО:
const members = JSON.parse(localStorage.getItem('members') || '[]');
const payments = JSON.parse(localStorage.getItem('payments') || '[]');

const rsvData = {
    employees: members.length,  // Реальное количество
    totalIncome: payments.reduce((sum, p) => sum + p.amount, 0)  // Реальная сумма
};
```

---

### 6. СЗВ-СТАЖ (showSZVSTAZHReport)

**Файл:** `index.html` (строка ~3450)

**ПРОБЛЕМА:** Примерные данные

**ЧТО ИСПРАВИТЬ:**

```javascript
// БЫЛО:
const employees = [
    { snils: '123-456-789 00', name: 'Иванов И.И.' }  // Примерно!
];

// СТАЛО:
const members = JSON.parse(localStorage.getItem('members') || '[]');

const employees = members.map(m => ({
    snils: m.snils || '—',  // Реальные данные
    name: m.name,
    period: `${new Date(m.createdAt).toLocaleDateString()} - ${new Date().toLocaleDateString()}`
}));
```

---

### 7. 6-НДФЛ (show6NDFLReport)

**Файл:** `index.html` (строка ~3510)

**ПРОБЛЕМА:** Примерные данные

**ЧТО ИСПРАВИТЬ:**

```javascript
// БЫЛО:
const ndflData = {
    employees: 3,  // Примерно!
    totalIncome: 450000  // Примерно!
};

// СТАЛО:
const members = JSON.parse(localStorage.getItem('members') || '[]');
const payments = JSON.parse(localStorage.getItem('payments') || '[]');

const ndflData = {
    employees: members.length,  // Реальное количество
    totalIncome: payments.reduce((sum, p) => sum + p.amount, 0),  // Реальная сумма
    taxAmount: Math.round(payments.reduce((sum, p) => sum + p.amount, 0) * 0.13)  // 13% НДФЛ
};
```

---

## 📊 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

| Отчёт | Критичность | Время | Статус |
|-------|-------------|-------|--------|
| **Баланс** | 🔴 Критично | 2 часа | ⏳ Требуется |
| **ОСВ** | 🔴 Критично | 2 часа | ⏳ Требуется |
| **КУДиР** | 🟡 Важно | 1 час | ✅ Частично |
| **Форма №2** | 🟡 Важно | 1 час | ✅ Частично |
| **РСВ** | 🟡 Важно | 1 час | ⏳ Требуется |
| **СЗВ-СТАЖ** | 🟡 Важно | 1 час | ⏳ Требуется |
| **6-НДФЛ** | 🟡 Важно | 1 час | ⏳ Требуется |

---

## 🎯 ПЛАН РАБОТЫ

### Шаг 1: Исправить Баланс (2 часа)

1. Получить данные из `transactions` и `payments`
2. Посчитать остатки по счетам
3. Сформировать Актив и Пассив
4. Проверить равенство

### Шаг 2: Исправить ОСВ (2 часа)

1. Получить данные из `transactions`
2. Сгруппировать по счетам
3. Посчитать обороты Дт/Кт
4. Посчитать остатки на начало/конец

### Шаг 3: Исправить налоговые отчёты (3 часа)

1. РСВ — реальные данные по сотрудникам
2. СЗВ-СТАЖ — реальные данные по пайщикам
3. 6-НДФЛ — реальные данные по доходам

---

## ✅ КРИТЕРИИ ПРИЁМКИ

**Отчёт считается исправленным, если:**

1. ✅ Использует данные из `localStorage`
2. ✅ Фильтрует за выбранный период
3. ✅ Считает итоги корректно
4. ✅ Проверяет равенство (для Баланса)
5. ✅ Показывает актуальные данные

---

**Готов приступить к исправлениям!** 🚀
