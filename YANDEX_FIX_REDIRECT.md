# ⚠️ СРОЧНО: ИСПРАВЛЕНИЕ ОШИБКИ REDIRECT_URI

**Ошибка:** `redirect_uri не совпадает с Callback URL`

---

## 🎯 ПРОБЛЕМА

Ваш текущий URL:
```
https://kkav45.github.io/cooperant/messenger_interface.html
```

В файле указан Redirect URI:
```
https://kkav45.github.io/cooperant/yandex-auth-callback.html
```

**Яндекс требует:** Эти URL должны **ТОЧНО** совпадать с тем, что указано в Яндекс Console!

---

## ✅ ЧТО СДЕЛАТЬ ПРЯМО СЕЙЧАС

### Шаг 1: Откройте Яндекс OAuth Console

Перейдите на: **https://oauth.yandex.ru/client/new**

Или выберите существующее приложение, если оно уже создано.

---

### Шаг 2: Проверьте/Обновите Redirect URI

В разделе **"Redirect URI"** укажите ТОЧНО:

```
https://kkav45.github.io/cooperant/yandex-auth-callback.html
```

⚠️ **ВАЖНО:**
- Только строчные буквы (нижний регистр)
- Без пробелов в конце
- Без косой черты `/` в конце
- HTTPS (не HTTP!)

---

### Шаг 3: Проверьте права доступа

Убедитесь, что выбрано:

- ✅ **cloud_api:disk.app_folder** (Яндекс.Диск)

Больше ничего не нужно!

---

### Шаг 4: Сохраните изменения

Нажмите кнопку **"Сохранить"** или **"Обновить"**

---

### Шаг 5: Закоммитьте изменения в GitHub

Откройте терминал в папке проекта:

```bash
cd "d:\!Минкооп_рф\! КООП"
git add .
git commit -m "Fix Yandex OAuth redirect URI"
git push origin main
```

Или через GitHub Desktop:
1. Откройте GitHub Desktop
2. Выберите изменения в файлах
3. Введите сообщение коммита
4. Нажмите "Commit & Push"

---

### Шаг 6: Подождите 1-2 минуты

GitHub Pages обновится автоматически.

---

### Шаг 7: Проверьте авторизацию

1. Откройте: https://kkav45.github.io/cooperant/messenger_interface.html
2. Нажмите на кнопку "Яндекс Диск"
3. Нажмите "Войти через Яндекс"
4. Должна открыться авторизация Яндекса

---

## 🔍 ПРОВЕРКА ПЕРЕД АВТОРИЗАЦИЕЙ

### 1. Проверьте файл yandex-disk-integration-v2.js

Откройте файл и найдите строку 48:

```javascript
REDIRECT_URI: 'https://kkav45.github.io/cooperant/yandex-auth-callback.html',
```

Должно быть **ТОЧНО ТАК**!

### 2. Проверьте Яндекс Console

1. Откройте https://oauth.yandex.ru/client/
2. Выберите ваше приложение
3. Проверьте поле "Redirect URI"
4. Должно быть: `https://kkav45.github.io/cooperant/yandex-auth-callback.html`

### 3. Проверьте callback страницу

Откройте в браузере:
```
https://kkav45.github.io/cooperant/yandex-auth-callback.html
```

Должна загрузиться страница с надписью "Обработка авторизации..."

---

## 🐛 ЕСЛИ ВСЁ ЕЩЁ НЕ РАБОТАЕТ

### Ошибка 1: "redirect_uri не совпадает"

**Проверьте:**

1. Откройте консоль браузера (F12)
2. Найдите строки:
   ```
   [Yandex] Текущий URL: https://kkav45.github.io/cooperant/messenger_interface.html
   [Yandex] Configured Redirect URI: https://kkav45.github.io/cooperant/yandex-auth-callback.html
   ```
3. Сравните с тем, что указано в Яндекс Console

**Возможные проблемы:**

| Проблема | Решение |
|----------|---------|
| `Cooperant` вместо `cooperant` | Используйте только строчные буквы |
| `KOOP` вместо `koop` | Регистр имеет значение! |
| Лишний `/` в конце | Уберите косую черту |
| `http://` вместо `https://` | Используйте только HTTPS |

---

### Ошибка 2: Страница callback не загружается

**Проверьте:**

1. Файл `yandex-auth-callback.html` существует в репозитории
2. Файл загружен на GitHub (проверьте на github.com)
3. GitHub Pages включён в настройках репозитория

**Решение:**

```bash
# Проверьте, что файл закоммичен
git status

# Если файла нет, добавьте его
git add yandex-auth-callback.html
git commit -m "Add Yandex callback page"
git push
```

---

### Ошибка 3: Авторизация открывается, но не возвращается

**Причина:** После авторизации Яндекс перенаправляет на callback, но токен не сохраняется

**Решение:**

1. Откройте консоль браузера (F12)
2. Пройдите авторизацию
3. Проверьте, есть ли сообщения:
   ```
   [Auth Callback] Hash: #access_token=...
   [Auth Callback] Token: получен
   ```
4. Если токена нет — проблема в Redirect URI

---

## 📊 КОНТРОЛЬНЫЙ СПИСОК

- [ ] Redirect URI в Яндекс Console: `https://kkav45.github.io/cooperant/yandex-auth-callback.html`
- [ ] Redirect URI в `yandex-disk-integration-v2.js`: строка 48
- [ ] Файл `yandex-auth-callback.html` загружен на GitHub
- [ ] GitHub Pages включён
- [ ] Издания закоммичены и отправлены на GitHub
- [ ] Прошли 1-2 минуты после push (обновление GitHub Pages)
- [ ] Страница callback загружается

---

## 🎯 БЫСТРАЯ ПРОВЕРКА

Выполните в консоли браузера:

```javascript
// Проверка конфигурации
console.log('Redirect URI:', YANDEX_DISK_CONFIG.REDIRECT_URI);

// Проверка токена
console.log('Token:', localStorage.getItem('yandexDiskToken'));

// Проверка callback страницы
fetch('yandex-auth-callback.html')
  .then(r => r.ok ? 'OK' : 'NOT FOUND')
  .then(console.log);
```

---

## ✅ УСПЕШНАЯ АВТОРИЗАЦИЯ

После правильной настройки вы увидите:

```
[Yandex] ✓ Яндекс Диск авторизация успешна
[Yandex] ✓ Папка КООПЕРАНТ найдена
[Yandex] ✓ Загрузка данных...
```

В Яндекс.Диске появится папка:
```
КООПЕРАНТ/
├── Data/
├── Applications/
├── Backup/
└── ...
```

---

## 📞 ЕСЛИ ВСЁ ЕЩЁ НЕ РАБОТАЕТ

1. **Сделайте скриншот** ошибки
2. **Откройте консоль** (F12) и скопируйте логи
3. **Проверьте** Яндекс Console ещё раз
4. **Попробуйте** в режиме инкогнито

---

**Дата:** 18 февраля 2026  
**Ваш URL:** https://kkav45.github.io/cooperant/messenger_interface.html  
**Redirect URI:** https://kkav45.github.io/cooperant/yandex-auth-callback.html
