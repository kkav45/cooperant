# РУКОВОДСТВО ПО РАЗВЁРТЫВАНИЮ TELEGRAM MINI APP + YANDEX DISK

**Версия:** 1.0  
**Дата:** 18 февраля 2026

---

## 📋 СОДЕРЖАНИЕ

1. [Требования](#требования)
2. [Настройка Яндекс.Диска](#настройка-яндексдиска)
3. [Настройка Telegram бота](#настройка-telegram-бота)
4. [Развёртывание приложения](#развёртывание-приложения)
5. [Безопасность](#безопасность)
6. [Тестирование](#тестирование)
7. [Устранение проблем](#устранение-проблем)

---

## 🔧 ТРЕБОВАНИЯ

### Обязательные

| Требование | Описание | Статус |
|------------|----------|--------|
| HTTPS | Обязательно для Telegram Mini App | ✅ |
| Яндекс OAuth | Приложение в Яндекс Console | ✅ |
| Telegram Bot | Бот в @BotFather | ✅ |
| Веб-хостинг | GitHub Pages, Vercel, Netlify | ✅ |

### Рекомендуемые

| Технология | Описание |
|------------|----------|
| GitHub Pages | Бесплатный хостинг с HTTPS |
| Yandex Cloud | Для бэкенд валидации initData |
| Node.js 18+ | Для локальной разработки |

---

## ☁️ НАСТРОЙКА ЯНДЕКС.ДИСКА

### Шаг 1: Создание OAuth приложения

1. Перейдите на [https://oauth.yandex.ru/client/new](https://oauth.yandex.ru/client/new)
2. Заполните данные приложения:

```
Название: Кооператив - Учёт пайщиков
Платформа: Веб-сервис
Redirect URI: https://YOUR_USERNAME.github.io/KOOP/
```

3. Выберите права доступа:
   - ✅ **Яндекс.Диск** → `cloud_api:disk.app_folder`

4. Запомните:
   - **Client ID** (ID приложения)
   - **Client Secret** (Секретный ключ)

### Шаг 2: Настройка конфигурации

Откройте файл `yandex-disk-integration-v2.js` и замените:

```javascript
const YANDEX_DISK_CONFIG = {
    CLIENT_ID: 'YOUR_CLIENT_ID',        // Вставьте из Яндекс Console
    CLIENT_SECRET: 'YOUR_CLIENT_SECRET', // Вставьте из Яндекс Console
    REDIRECT_URI: 'https://YOUR_USERNAME.github.io/KOOP/',
    FOLDER_NAME: 'КООПЕРАНТ',
    AUTO_SAVE_INTERVAL: 30000,
    MAX_BACKUP_COUNT: 10,
    COMPRESSION_ENABLED: true
};
```

### Шаг 3: Проверка прав доступа

После первой авторизации проверьте в Яндекс.Диске:
- ✅ Создана папка `КООПЕРАНТ`
- ✅ Внутри папки созданы подпапки: `Data`, `Applications`, `Backup`, etc.

---

## 🤖 НАСТРОЙКА TELEGRAM БОТА

### Шаг 1: Создание бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Введите имя и username бота:
   ```
   Name: Кооператив Учёт
   Username: KooperativUchetBot
   ```
4. Сохраните **API Token**

### Шаг 2: Настройка Mini App

1. В @BotFather отправьте `/mybots`
2. Выберите вашего бота
3. `Bot Settings` → `Menu Button`
4. `Configure Menu Button`
5. Отправьте URL вашего приложения:
   ```
   https://YOUR_USERNAME.github.io/KOOP/telegram-messenger-interface.html
   ```
6. Введите название кнопки: `Открыть приложение`

### Шаг 3: Дополнительные настройки

```
Bot Settings → Description
Введите описание бота для пользователей

Bot Settings → About
Введите информацию о боте

Bot Settings → Inline Mode (опционально)
Включите для inline-запросов
```

---

## 🚀 РАЗВЁРТЫВАНИЕ ПРИЛОЖЕНИЯ

### Вариант 1: GitHub Pages (рекомендуется)

#### Шаг 1: Подготовка репозитория

```bash
# Создайте репозиторий на GitHub
# Например: https://github.com/YOUR_USERNAME/KOOP

# Инициализируйте локальный репозиторий
git init
git add .
git commit -m "Initial commit: Telegram Mini App + Yandex Disk"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/KOOP.git
git push -u origin main
```

#### Шаг 2: Включение GitHub Pages

1. Перейдите в репозиторий на GitHub
2. `Settings` → `Pages`
3. Source: `Deploy from a branch`
4. Branch: `main` / `root`
5. Save

#### Шаг 3: Проверка

Через 1-2 минуты приложение будет доступно по URL:
```
https://YOUR_USERNAME.github.io/KOOP/
```

### Вариант 2: Vercel

```bash
# Установите Vercel CLI
npm install -g vercel

# Разверните приложение
cd "d:\!Минкооп_рф\! КООП"
vercel --prod
```

### Вариант 3: Netlify

```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Разверните приложение
cd "d:\!Минкооп_рф\! КООП"
netlify deploy --prod --dir="."
```

---

## 🔒 БЕЗОПАСНОСТЬ

### Валидация initData Telegram

**ВАЖНО:** Полная валидация должна выполняться на бэкенде!

#### Фронтенд (базовая проверка)

```javascript
// В файле yandex-disk-integration-v2.js
function validateTelegramInitData(initData) {
    if (!initData) return false;
    
    const urlParams = new URLSearchParams(initData);
    const authDate = new Date(parseInt(urlParams.get('auth_date')) * 1000);
    const now = new Date();
    
    // Проверяем, не старше 24 часов
    const hoursDiff = (now - authDate) / (1000 * 60 * 60);
    
    if (hoursDiff > 24) {
        return false;
    }
    
    // Проверяем наличие hash
    if (!urlParams.get('hash')) {
        return false;
    }
    
    return true;
}
```

#### Бэкенд (Node.js пример)

```javascript
const crypto = require('crypto');

function validateTgInitData(initData, botToken) {
    const urlParams = new URLSearchParams(initData);
    const hash = urlParams.get('hash');
    urlParams.delete('hash');
    
    // Сортируем параметры
    const sortedParams = Array.from(urlParams.entries())
        .sort(([a], [b]) => a.localeCompare(b))
        .map(([key, value]) => `${key}=${value}`)
        .join('\n');
    
    // Создаём ключ подписи
    const secret = crypto
        .createHmac('sha256', 'WebAppData')
        .update(botToken)
        .digest();
    
    // Создаём hash
    const calculatedHash = crypto
        .createHmac('sha256', secret)
        .update(sortedParams)
        .digest('hex');
    
    return calculatedHash === hash;
}

// Использование
const isValid = validateTgInitData(initData, process.env.BOT_TOKEN);
```

### Защита данных Яндекс.Диска

1. **Ограничьте права доступа** — только `cloud_api:disk.app_folder`
2. **Не храните Client Secret** в коде фронтенда
3. **Используйте HTTPS** для всех запросов
4. **Регулярно создавайте резервные копии**

---

## 🧪 ТЕСТИРОВАНИЕ

### Чек-лист тестирования

#### Telegram Mini App

- [ ] Приложение открывается в Telegram
- [ ] ThemeParams применяются (светлая/тёмная тема)
- [ ] MainButton отображается и работает
- [ ] BackButton синхронизирован с навигацией
- [ ] HapticFeedback работает на поддерживаемых устройствах
- [ ] Viewport адаптируется при открытии клавиатуры
- [ ] Safe area insets учитываются (iPhone X+)

#### Яндекс.Диск

- [ ] Авторизация проходит успешно
- [ ] Папка `КООПЕРАНТ` создаётся
- [ ] Данные загружаются из Яндекс.Диска
- [ ] Данные сохраняются в Яндекс.Диск
- [ ] Автосохранение работает (каждые 30 сек)
- [ ] Резервные копии создаются
- [ ] Старые резервные копии удаляются

#### Интеграция

- [ ] Данные синхронизируются корректно
- [ ] Конфликты синхронизации обрабатываются
- [ ] Offline-режим работает (localStorage)
- [ ] Повторное подключение работает

### Тестовый сценарий

```
1. Откройте бота в Telegram
2. Нажмите "Открыть приложение"
3. Проверьте применение темы Telegram
4. Нажмите "Подключить Яндекс.Диск"
5. Пройдите авторизацию
6. Создайте тестового пайщика
7. Проверьте создание файла на Яндекс.Диске
8. Обновите страницу
9. Проверьте загрузку данных
10. Измените данные
11. Проверьте автосохранение (30 сек)
```

---

## 🔧 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: Приложение не открывается в Telegram

**Решение:**
1. Проверьте HTTPS соединение
2. Убедитесь, что URL указан правильно в @BotFather
3. Проверьте консоль браузера на ошибки

### Проблема: Авторизация Яндекс.Диска не работает

**Решение:**
1. Проверьте Client ID в настройках
2. Убедитесь, что Redirect URI совпадает
3. Проверьте права доступа в Яндекс Console
4. Попробуйте отозвать и выдать права заново

### Проблема: Данные не синхронизируются

**Решение:**
1. Проверьте токен авторизации
2. Проверьте наличие папки `КООПЕРАНТ`
3. Проверьте консоль на ошибки сети
4. Попробуйте принудительную синхронизацию:
   ```javascript
   forceSyncYandex();
   ```

### Проблема: ThemeParams не применяются

**Решение:**
1. Проверьте загрузку `telegram-web-app.js`
2. Убедитесь, что `Telegram.WebApp.ready()` вызван
3. Проверьте `window.TelegramMiniApp.init()`
4. Обновите страницу

### Проблема: HapticFeedback не работает

**Решение:**
1. Проверьте, что устройство поддерживает
2. Убедитесь, что вызывается из обработчика клика
3. Проверьте версию Telegram клиента

---

## 📊 МОНИТОРИНГ

### Логирование

Включите расширенное логирование:

```javascript
// В начале файла yandex-disk-integration-v2.js
window.YANDEX_DEBUG = true;

// При инициализации
Logger.info('Debug mode enabled');
```

### Статус синхронизации

```javascript
// Получить статус
const status = getYandexSyncStatus();
console.log(status);

// Обновить UI
updateYandexStatusUI();
```

### Метрики для отслеживания

| Метрика | Норма | Критично |
|---------|-------|----------|
| First load time | < 3 сек | > 5 сек |
| Sync time | < 2 сек | > 10 сек |
| Auth success rate | > 95% | < 80% |
| Auto-save success | 100% | < 90% |

---

## 📱 МОБИЛЬНАЯ АДАПТАЦИЯ

### Проверка на устройствах

| Устройство | Проверка |
|------------|----------|
| iPhone X+ | Safe area insets |
| Android | Клавиатура |
| iPad | Многооконность |
| Desktop | Telegram Desktop |

### Рекомендации

1. **Минимальная высота touch targets:** 44px
2. **Шрифты:** Используйте системные Telegram
3. **Цвета:** Адаптируйте через themeParams
4. **Анимации:** Плавные, < 300ms

---

## 🔄 ОБНОВЛЕНИЕ ПРИЛОЖЕНИЯ

### Автоматическое обновление

При использовании GitHub Pages:

```bash
git pull origin main
git push
# GitHub Pages обновится автоматически
```

### Принудительное обновление у пользователей

```javascript
// Проверка версии
const APP_VERSION = '1.0.0';
const storedVersion = localStorage.getItem('appVersion');

if (storedVersion !== APP_VERSION) {
    // Очищаем кэш
    localStorage.clear();
    // Перезагружаем
    location.reload(true);
}
```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### Документация

- [Telegram Web Apps SDK](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Яндекс.Диск REST API](https://yandex.ru/dev/disk/)
- [Яндекс OAuth](https://yandex.ru/dev/oauth/)

### Инструменты

- [Telegram BotFather](https://t.me/BotFather)
- [Яндекс OAuth Console](https://oauth.yandex.ru/client/new)
- [GitHub Pages](https://pages.github.com/)
- [Vercel](https://vercel.com/)

### Сообщества

- [Telegram Bot Developers](https://t.me/BotTalk)
- [Яндекс.Диск API](https://yandex.ru/dev/forum/)

---

## ✅ ЧЕК-ЛИСТ ПЕРЕД РЕЛИЗОМ

### Техническая готовность

- [ ] HTTPS настроен
- [ ] Telegram Mini App интегрирован
- [ ] Яндекс.Диск авторизация работает
- [ ] Автосохранение включено
- [ ] Резервные копии создаются
- [ ] ThemeParams применяются
- [ ] HapticFeedback работает
- [ ] Safe area insets учтены

### Безопасность

- [ ] initData валидация (бэкенд)
- [ ] Client Secret не в коде
- [ ] HTTPS для всех запросов
- [ ] CORS настроен
- [ ] Нет чувствительных данных в localStorage

### UX/UI

- [ ] Все кнопки ≥ 44px
- [ ] Контрастность ≥ 4.5:1
- [ ] Нет горизонтального скролла
- [ ] Анимации плавные
- [ ] Ошибки обрабатываются
- [ ] Загрузка показывается

### Документация

- [ ] README.md обновлён
- [ ] Инструкция по развёртыванию
- [ ] API документация
- [ ] Changelog ведётся

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Протестировать** на реальных устройствах
2. **Настроить бэкенд** для валидации initData
3. **Добавить аналитику** (Telegram WebApp Analytics)
4. **Оптимизировать** производительность
5. **Пройти модерацию** Telegram

---

**Контакты для поддержки:** документация в файле `README.md`
