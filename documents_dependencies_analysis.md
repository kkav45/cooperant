# 📊 АНАЛИЗ ЗАВИСИМОСТЕЙ ДОКУМЕНТОВ

## Классификация по способу формирования

---

## ТИП 1: ДОКУМЕНТЫ, ФОРМИРУЕМЫЕ ИЗ ФОРМ ВВОДА

Эти документы создаются пользователем путём заполнения форм в системе.

### 1.1 Первичные документы

| Документ | Форма ввода | Исходные данные |
|----------|-------------|-----------------|
| **ПКО** (Приходный кассовый ордер) | `createPKO()` | Ввод: сумма, пайщик, тип взноса, основание |
| **РКО** (Расходный кассовый ордер) | `createRKO()` | Ввод: сумма, получатель, основание, документ |
| **Бухгалтерская справка** | `createAccountingCertificate()` | Ввод: Дебет, Кредит, сумма, описание |

**Особенности:**
- Создаются вручную бухгалтером
- Требуют заполнения всех полей
- После создания формируют бухгалтерские проводки
- Печатаются по утверждённым формам

---

### 1.2 Протоколы и собрания

| Документ | Форма ввода | Исходные данные |
|----------|-------------|-----------------|
| **Протокол общего собрания** | `showMeetingProtocolForm()` | Ввод: дата, место, кворум, повестка, подписи |
| **Лист регистрации участников** | `showAttendanceList()` | Авто: список активных пайщиков + ручной ввод подписей |

**Особенности:**
- Протокол заполняется секретарём собрания
- Лист регистрации печатается заранее, подписи ставятся вручную

---

### 1.3 Заключения и отчёты с вводом данных

| Документ | Форма ввода | Исходные данные |
|----------|-------------|-----------------|
| **Заключение ревизионной комиссии** | `showSupervisionReport()` | Ввод: период, нарушения, рекомендации, тип заключения + авто: данные проверок |

**Особенности:**
- Частично автоматическое формирование
- Требует ввода оценочных суждений ревизора

---

## ТИП 2: ДОКУМЕНТЫ, ФОРМИРУЕМЫЕ НА ОСНОВЕ ДАННЫХ СИСТЕМЫ

Эти документы формируются автоматически на основе накопленных данных.

### 2.1 Реестры (полностью автоматические)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Реестр членов кооператива** | `showMembersRegistry()` | Таблица `members` (все пайщики) |
| **Реестр выданных удостоверений** | `showCertificatesRegistry()` | Таблица `certificates` + `members` |

**Исходные данные:**
```javascript
// Реестр членов
members.filter(m => m.status === 'active')
  .map(m => ({ ФИО, дата вступления, участок, контакт }));

// Реестр удостоверений
certificates.map(c => ({ номер, дата, пайщик, статус }));
```

---

### 2.2 Отчёты по фондам (расчётные)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Годовой отчёт по фондам** | `showAnnualFundsReport()` | Таблица `transactions` (проводки по счетам 86-1..86-5) |
| **Ведомость движения фондов** | `showFundsMovementReport()` | Таблица `transactions` (обороты за период) |

**Формулы расчёта:**
```javascript
// Остаток фонда на дату
calculateFundBalance(account, date) = 
    Credit(account, до даты) - Debit(account, до даты)

// Обороты за период
calculateFundTurnover(account, year, side) = 
    Sum(transactions где account и период)
```

---

### 2.3 Карточки пайщиков (выборка данных)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Карточка пайщика** | `showMemberCard(memberId)` | `members[id]` + `payments.filter(memberId)` + `calculateShareValue()` |
| **Карточка паевого взноса** | `showSharePaymentCard(memberId)` | `payments.filter(memberId)` + группировка по типам |

**Исходные данные:**
```javascript
// Карточка пайщика
{
    info: members.find(m => m.id === memberId),
    payments: payments.filter(p => p.memberId === memberId),
    balance: totalPaid - totalReturned,
    shareValue: calculateShareValue()
}

// Карточка взноса
{
    byType: payments.reduce((acc, p) => {
        acc[p.type] += p.amount;
    }, {}),
    history: payments.filter(p => p.memberId === memberId)
}
```

---

### 2.4 Аналитические отчёты (агрегация)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Аналитические отчёты** | `showAnalyticsReports()` | `members` (динамика), `payments` (поступления, задолженности) |
| **Отчёт по паевым взносам** | `generatePaymentsReport()` | `payments` (все взносы) |
| **Отчёт о задолженностях** | `generateDebtReport()` | `payments.filter(!paid)` |
| **Отчёт о возвратах** | `generateShareReturnReport()` | `payments.filter(type === 'return_share')` |

**Формулы расчёта:**
```javascript
// Динамика численности по месяцам
for (month of 12) {
    count = members.filter(m => 
        m.status === 'active' && 
        joinDate <= monthEnd
    ).length;
}

// Поступления по месяцам
for (month of 12) {
    income = payments.filter(p => 
        p.paid && 
        date >= monthStart && 
        date <= monthEnd
    ).reduce(sum + amount, 0);
}

// Задолженность
debt = payments.filter(p => !p.paid)
    .reduce((sum, p) => sum + p.amount, 0);
```

---

### 2.5 Финансовые отчёты (бухгалтерские)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Бухгалтерский баланс** | `showOfficialBalanceSheet()` | `transactions` (счета 01, 08, 10, 41, 50, 51, 60, 62, 76, 86) |
| **Отчёт о фин. результатах** | `showOfficialProfitLossStatement()` | `transactions` (счета 90, 91) |
| **Отчёт о целевом использовании** | `showTargetUseReport()` | `transactions` (счета 86) |
| **Журнал целевого использования** | `showTargetUseJournal()` | `transactions` (счета 86-1..86-5) |

**Формулы расчёта:**
```javascript
// Бухгалтерский баланс
{
    assets: {
        nonCurrent: transactions.filter(t => 
            t.debitAccount.startsWith('01') || 
            t.debitAccount.startsWith('08')
        ).reduce(sum + amount, 0),
        current: {
            cash: transactions.filter(t => 
                t.debitAccount.startsWith('50') || 
                t.debitAccount.startsWith('51')
            ).reduce(sum + amount, 0),
            receivables: transactions.filter(t => 
                t.debitAccount.startsWith('62') || 
                t.debitAccount.startsWith('76')
            ).reduce(sum + amount, 0)
        }
    },
    equity: {
        shareCapital: transactions.filter(t => 
            t.creditAccount === '86-1'
        ).reduce(sum + amount, 0),
        indivisibleFund: transactions.filter(t => 
            t.creditAccount === '86-2'
        ).reduce(sum + amount, 0),
        // ... другие фонды
    }
}

// Отчёт о фин. результатах
{
    revenue: transactions.filter(t => 
        t.creditAccount.startsWith('90') || 
        t.creditAccount.startsWith('91')
    ).reduce(sum + amount, 0),
    expenses: transactions.filter(t => 
        t.debitAccount.startsWith('90-2') || 
        t.debitAccount.startsWith('26') || 
        t.debitAccount.startsWith('44')
    ).reduce(sum + amount, 0),
    profit: revenue - expenses
}
```

---

### 2.6 Налоговые отчёты (расчётные)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Декларация УСН** | `showUSNDeclaration()` | `taxAccounting` (доходы/расходы) + `taxSystem` |
| **КУДиР** | `showKUDiR()` | `taxAccounting.income` + `taxAccounting.expense` |

**Формулы расчёта:**
```javascript
// Налоговая база
taxBase = calculateTaxBase(year) = {
    totalIncome: income.filter(r => year)
        .reduce(sum + amount, 0),
    totalExpense: expense.filter(r => year)
        .reduce(sum + amount, 0),
    taxBase: taxSystem === 'USN_15' 
        ? Math.max(0, income - expense) 
        : income
}

// Налог
taxCalc = calculateUSNTax(year) = {
    taxRate: taxSystem === 'USN_15' ? 0.15 : 0.06,
    calculatedTax: taxBase * taxRate,
    minTax: taxSystem === 'USN_15' ? income * 0.01 : 0,
    taxToPay: max(calculatedTax, minTax)
}
```

---

### 2.7 Календарь и контроль (смешанные)

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Календарь событий** | `showCalendar()` | `calendarEvents` (ввод + авто из сроков) |
| **Контроль сроков** | `showControlDashboard()` | `documents` + `controlSettings.documentRetention` + `controlSettings.reportingDeadlines` |

**Формулы расчёта:**
```javascript
// Истекающие документы
expiringDocs = documents.filter(doc => {
    expiryDate = calculateRetentionExpiryDate(doc.date, doc.type);
    return expiryDate <= thresholdDate; // 90 дней
});

// Предстоящие сроки отчётности
upcomingDeadlines = Object.entries(deadlines).map(([type, date]) => {
    reportDate = new Date(currentYear, month, day);
    daysUntil = (reportDate - now) / (1000 * 60 * 60 * 24);
    return { type, date, daysUntil };
}).filter(d => daysUntil <= 90);
```

---

### 2.8 Расчёт стоимости пая

| Документ | Функция | Источник данных |
|----------|---------|-----------------|
| **Расчёт стоимости пая** | `showShareValueCalculation()` | `transactions` (активы, обязательства, фонды) + `members` (количество) |

**Формула расчёта:**
```javascript
// Действительная стоимость пая
shareValue = calculateShareValue() = {
    netAssets: assets - liabilities,
    indivisibleFund: transactions.filter(t => 
        t.creditAccount === '86-2'
    ).reduce(sum + amount, 0),
    reserveFund: transactions.filter(t => 
        t.creditAccount === '86-3' || 
        t.creditAccount === '82'
    ).reduce(sum + amount, 0),
    shareMass: netAssets - indivisibleFund - reserveFund,
    activeMembersCount: members.filter(m => 
        m.status === 'active'
    ).length,
    shareValue: shareMass / activeMembersCount
}
```

---

## ТИП 3: ШАБЛОНЫ (нулевая отчётность)

Эти документы формируются по шаблонам при отсутствии деятельности.

| Документ | Функция | Основа |
|----------|---------|--------|
| **Нулевая декларация УСН** | `createZeroUSNDeclaration()` | Шаблон с нулевыми показателями |
| **Нулевой бухгалтерский баланс** | `createZeroBalanceSheet()` | Шаблон с нулевыми показателями |

**Особенности:**
- Не требуют данных из системы
- Используют только реквизиты организации
- Печатаются по утверждённым формам

---

## 📊 СВОДНАЯ ТАБЛИЦА ЗАВИСИМОСТЕЙ

| Документ | Тип | Форма ввода | Автоданные | Расчёты |
|----------|-----|-------------|------------|---------|
| ПКО | 1 | ✅ | — | — |
| РКО | 1 | ✅ | — | — |
| Бухгалтерская справка | 1 | ✅ | — | ✅ (проводки) |
| Протокол собрания | 1 | ✅ | ✅ (кворум) | ✅ (авто) |
| Лист регистрации | 1 | ✅ | ✅ (список) | — |
| Заключение ревизии | 1 | ✅ | ✅ (проверки) | — |
| Реестр членов | 2 | — | ✅ | — |
| Реестр удостоверений | 2 | — | ✅ | — |
| Годовой отчёт по фондам | 2 | — | ✅ | ✅ (остатки) |
| Ведомость движения фондов | 2 | — | ✅ | ✅ (обороты) |
| Карточка пайщика | 2 | — | ✅ | ✅ (баланс) |
| Карточка взноса | 2 | — | ✅ | ✅ (структура) |
| Аналитические отчёты | 2 | — | ✅ | ✅ (агрегация) |
| Отчёт по взносам | 2 | — | ✅ | ✅ (суммы) |
| Отчёт о задолженностях | 2 | — | ✅ | ✅ (суммы) |
| Отчёт о возвратах | 2 | — | ✅ | ✅ (суммы) |
| Бухгалтерский баланс | 2 | — | ✅ | ✅ (счета) |
| Отчёт о фин. результатах | 2 | — | ✅ | ✅ (прибыль) |
| Отчёт о целевом использовании | 2 | — | ✅ | ✅ (целевые) |
| Журнал целевого использования | 2 | — | ✅ | — |
| Декларация УСН | 2 | — | ✅ | ✅ (налог) |
| КУДиР | 2 | — | ✅ | — |
| Календарь событий | 2 | ✅ | ✅ | — |
| Контроль сроков | 2 | — | ✅ | ✅ (даты) |
| Расчёт стоимости пая | 2 | — | ✅ | ✅ (формула) |
| Нулевая декларация УСН | 3 | ✅ | — | — |
| Нулевой баланс | 3 | ✅ | — | — |

---

## 🎯 ЦЕПОЧКИ ФОРМИРОВАНИЯ ДОКУМЕНТОВ

### Цепочка 1: Первичный документ → Проводка → Отчёт

```
ПКО/РКО (форма)
    ↓
Бухгалтерская проводка (transactions)
    ↓
Бухгалтерский баланс / Отчёт о фин. результатах
```

### Цепочка 2: Взнос → Карточка → Отчёт

```
Взнос (payments)
    ↓
Карточка пайщика / Карточка взноса
    ↓
Отчёт по взносам / Аналитика
```

### Цепочка 3: Документ → Контроль сроков

```
Документ (documents)
    ↓
Расчёт срока хранения (controlSettings)
    ↓
Контроль сроков (истекающие)
```

### Цепочка 4: Событие → Календарь

```
Событие (calendarEvents)
    ↓
Календарь событий
    ↓
Предстоящие события (30 дней)
```

### Цепочка 5: Проводка → Налоговый учёт → Декларация

```
Хозяйственная операция
    ↓
Бухгалтерская проводка
    ↓
Классификация для УСН (taxAccounting)
    ↓
КУДиР → Декларация УСН
```

---

## 📋 ВЫВОДЫ

### Документы типа 1 (формы ввода): 6
- Требуют ручного заполнения
- Создают новые записи в системе
- Формируют первичные данные

### Документы типа 2 (автоформирование): 26
- Используют накопленные данные
- Требуют расчётов и агрегации
- Формируют отчётность

### Документы типа 3 (шаблоны): 2
- Не требуют данных
- Используют реквизиты организации
- Для случаев отсутствия деятельности

---

**Версия:** 1.0
**Дата:** 16 февраля 2026 г.
**Статус:** ✅ Проанализировано 34 документа
